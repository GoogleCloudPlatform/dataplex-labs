config { 
  type: "table",
  schema: "documentation_impact_analysis",
  tags: ["impact-analysis"]
}

SELECT
    npi,
    SUM(IF(is_medicare_participating_bool, avg_payment_ratio, 0) * avg_description_length) AS total_participation_index
FROM
  ${ref("supplier_agg1")}
GROUP BY
  npi
