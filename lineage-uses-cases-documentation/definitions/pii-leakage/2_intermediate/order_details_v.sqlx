config { 
    type: "view",
    schema: "documentation_pii_leakage",
    tags: ["pii-leakage"],
}

SELECT
    t1.order_id,
    t1.status AS order_status,
    t2.user_id
FROM
    ${ref("stg_orders")} AS t1
INNER JOIN
    ${ref("stg_order_items")} AS t2 ON t1.order_id = t2.order_id
